import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Activity2-AppDev',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
